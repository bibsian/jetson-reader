services:
  reading-assistant-dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    container_name: reading-assistant-dev
    ports:
      - "2222:22"
      - "8888:8888"
    volumes:
      - ~/Desktop/reading-assistant:/workspace
    restart: unless-stopped
